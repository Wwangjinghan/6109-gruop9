import { DashboardClient } from "./DashboardClient";

export const metadata = { title: "Dashboard — AgentIntent Protocol" };

export default function DashboardPage() {
  return <DashboardClient />;
}
